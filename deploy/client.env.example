# Client build-time env (only needed for SPLIT hosting — Vercel/Netlify).
# With single-service hosting (STATIC_DIR) leave this unset: the app uses
# same-origin relative /api paths automatically.
VITE_SERVER_URL=https://your-api.onrender.com
