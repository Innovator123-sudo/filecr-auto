# Vercel unified deployment: same-origin API (music + health).
# Empty => fetch("/api/music/...") hits Vercel's api/index.ts serverless function – zero CORS, no cold sleep.
VITE_SERVER_URL=
# Friend Battles need persistent WebSockets -> Vercel serverless can't hold Socket.IO state,
# so battles ride the Render backend below while Music/TURN stay same-origin on Vercel.
VITE_SOCKET_URL=https://pushup-pro-server-s22x.onrender.com
