services:
  - type: web
    name: pushup-pro-server
    env: node
    plan: free
    buildCommand: cd server && npm install && npm run build
    startCommand: cd server && npm start
    healthCheckPath: /health
    envVars:
      - key: CLIENT_ORIGIN
        value: https://client-gilt-three-81.vercel.app,https://client-kr5nowbnd-dfqwwq.vercel.app,http://localhost:5173
